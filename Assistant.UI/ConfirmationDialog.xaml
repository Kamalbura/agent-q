<Window x:Class="Assistant.UI.ConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirm Actions"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Topmost="True"
        Background="#FF1E1E1E"
        Foreground="White">
    <Border Padding="16" CornerRadius="8" Background="#FF2B2B2B">
        <StackPanel>
            <TextBlock Text="Astra needs your confirmation" FontSize="16" FontWeight="Bold" Margin="0,0,0,12" />
            <TextBlock x:Name="FocusedWindowText" Margin="0,0,0,12" />
            <ItemsControl x:Name="ItemsList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,4,0,4" Padding="8" Background="#33111111" CornerRadius="4">
                            <TextBlock Text="{Binding}" TextWrapping="Wrap" />
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button Content="Cancel" Width="80" Margin="0,0,8,0" Click="OnCancel" />
                <Button Content="Confirm" Width="100" IsDefault="True" Click="OnConfirm" />
            </StackPanel>
        </StackPanel>
    </Border>
</Window>